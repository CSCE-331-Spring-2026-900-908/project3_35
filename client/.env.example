# Backend origin for the Vite dev server proxy (/api -> this server)
VITE_API_ORIGIN=http://localhost:3001

# Deployed API origin for production builds (no trailing slash). Omit for local dev.
# VITE_API_BASE_URL=https://your-api.vercel.app
